# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MiracleBC_Staff' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MiracleBC_Staff
   pod "Moya"
  pod 'ObjectMapper'
  pod 'Moya-ObjectMapper', :git => 'https://github.com/bmoliveira/Moya-ObjectMapper.git', :branch => "master"

end
